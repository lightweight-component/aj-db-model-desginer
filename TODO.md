# 功能待办

本文档记录 AJ DB Model Designer 相对 drawDB 尚未覆盖或需要深化的能力。

## P1：本地多图管理与全量备份

### 多图管理

- 支持创建、打开、重命名、删除多个本地图。
- 使用 IndexedDB 保存图列表及每张图的元信息，包括名称、创建时间、最后修改时间和缩略图。
- 提供图列表的搜索、排序和最近打开记录。
- 保持当前自动保存与异常恢复能力，不覆盖未保存编辑。

验收标准：用户可以在不依赖后端的情况下创建、切换、删除多个图，并在刷新浏览器后恢复全部图列表及内容。

### 全量数据备份与恢复

- 导出全部本地图为一个版本化 JSON 备份文件。
- 导入备份时先校验版本、图数据和名称冲突。
- 支持“合并导入”及“覆盖当前本地图集合”两种明确操作。

验收标准：导出的备份可在另一浏览器实例完整恢复多张图，非法文件不会改写现有数据。

## P2：导出一致性与可配置性

### 精确画布图片导出

- 让 SVG、PNG、JPEG、WebP 导出复用实际画布的主题、表折叠状态、关系线路由和基数标签。
- 提供导出倍率、背景色和是否包含网格的选项。
- 大型图导出时提供处理中状态与失败提示。

验收标准：导出结果与当前画布视觉状态一致，且在大型图中不会无反馈地失败。

### 导出格式维护

- 继续维护 JSON、DBML、Mermaid、Markdown 和图片导出。
- PDF 保持暂不实现，后续如有需求再单独评估依赖与分页策略。

## P2：模型与 SQL 方言深化

### 结构化自定义类型

- 支持为 Custom Type 定义多个子字段，而不只是类型别名。
- 明确各方言下结构化类型的 SQL 生成策略及不支持时的提示。
- 保留类型字段编辑、排序、引用追踪和安全重命名能力。

验收标准：结构化类型可以创建、编辑、被字段引用，并在导入导出中稳定保留。

### SQL 方言细节

- 深化 PostgreSQL 数组、域类型和枚举的表达。
- 补齐 Oracle、MariaDB、Generic 的特有类型及约束差异。
- 对无法无损生成的 SQL 特性显示明确兼容性提示。

验收标准：各方言常见 DDL 导入后能够稳定编辑、再导出，并由自动化测试覆盖。

## P3：国际化与辅助体验

### 多语言与 RTL

- 抽取界面文案为语言包，优先支持中文与英文切换。
- 为 RTL 语言预留布局和关系方向适配能力。

验收标准：中英文界面切换不刷新页面，主要编辑路径无遗漏文案。

### 编辑器细节

- 评估全局表宽设置、导出前预览和更细粒度快捷键设置。
- 持续优化大型图性能、键盘可达性及窄视口体验。

## 后置：需要外部服务或更高产品复杂度的能力

- AI 辅助导入：将自然语言或不完整 SQL 转为图模型。
- 分享链接、只读模式与协作编辑。
- 版本快照、时间线、版本比较与迁移 SQL 生成。

这些能力涉及后端、鉴权、数据归属或较高维护成本；在本地编辑器体验稳定后再排期。

## 明确不在当前范围

- PDF 导出。
- drawDB 旧版 JSON 格式兼容迁移。
